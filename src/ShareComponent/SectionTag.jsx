
// eslint-disable-next-line react/prop-types
const SectionTag = ({text}) => {
    return (
        <p className=' w-fit bg-transparent  p-3 px-5 border-[1px] border-black rounded-full text-lg flex items-center justify-center gap-1 font-semibold'>{text}</p>
    );
};

export default SectionTag;